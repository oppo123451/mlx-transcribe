# verbatim

Local audio transcription with voice-activity detection and hallucination filtering. Runs Whisper large-v3 on Apple Silicon via MLX — no cloud, no API keys, no audio leaving the machine.

